[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    integration: live notebook execution via nbclient (requires kernel, slow)

# Run only the fast structural/contract tests in default CI:
#   pytest
# Run the full suite including notebook execution:
#   pytest -m integration --timeout=300
